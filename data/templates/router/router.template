const { BaseRouter } = require("emvicify/routers");

class {{ className }}Router extends BaseRouter {
    constructor({ controllers, middlewares }) {
        super({ controllers, middlewares });
    }

    register(app) {
        // app.get("/", (req, res) => this.controllers.{{ className | decapitalize }}.getList({ req, res }));
    }
}

module.exports = {{ className }}Router;
